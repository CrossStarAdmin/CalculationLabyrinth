# 04 フロア データ定義

## 1. 概要

{このドメインのデータをどこに保存するか}

| 保存場所 | 保存するデータ |
| --- | --- |
| サーバー | {内容} |
| ローカル | {内容} |

## 2. サーバー側のデータ

- **コレクション名**: `{コレクション名}`

### 2.1 データ構造

```
{コレクション名}/{ドキュメントID} {
    {項目名}: {型}
}
```

### 2.2 初期値

```json
{
    "{項目名}": {値}
}
```

### 2.3 権限

| 操作 | 許可する対象 | 条件 |
| --- | --- | --- |
| 読み | {対象} | {条件} |
| 書き | {対象} | {条件} |

### 2.4 各項目の説明

#### {項目名}

| 項目 | 内容 |
| --- | --- |
| 型 | {型} |
| 説明 | {説明} |
| 制約 | {制約} |
| 初期値 | {初期値} |
| 値の変更 | {いつ・どの画面で変わるか} |
| 書き権限 | {サーバー / アプリ} |

## 3. ローカル側のデータ

### 3.1 データ構造

```
{名前} {
    {項目名}: {型}
}
```

### 3.2 初期値

```json
{
    "{項目名}": {値}
}
```

### 3.3 各項目の説明

#### {項目名}

| 項目 | 内容 |
| --- | --- |
| 型 | {型} |
| 説明 | {説明} |
| 制約 | {制約} |
| 初期値 | {初期値} |
| 値の変更 | {いつ・どの画面で変わるか} |
| 書き権限 | {サーバー / アプリ} |
